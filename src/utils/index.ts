export { goPageG, historyBrowser, historyHash } from './goPage';
export { hasAuth } from './hasAuth';
export {
  getLocal,
  getSession,
  setLocal,
  setSession,
  removStorage,
  clearStorage
} from './storage';
